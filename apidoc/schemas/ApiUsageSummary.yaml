CreateApiUsageSummaryInput:
  type: object
  properties:
    start_date:
      type: string
      format: date-time
    end_date:
      type: string
      format: date-time
    total_calls:
      type: integer
    total_cost:
      type: number
      format: float
    subscription_id:
      type: integer
    api_endpoint_id:
      type: integer
    organization_id:
      type: integer
  required:
    - start_date
    - end_date
    - total_calls
    - total_cost
    - subscription_id
    - api_endpoint_id
    - organization_id

CreateApiUsageSummaryOutput:
  type: object
  properties:
    id:
      type: integer
    start_date:
      type: string
      format: date-time
    end_date:
      type: string
      format: date-time
    total_calls:
      type: integer
    total_cost:
      type: number
      format: float
    subscription_id:
      type: integer
    api_endpoint_id:
      type: integer
    organization_id:
      type: integer
  required:
    - id
    - start_date
    - end_date
    - total_calls
    - total_cost
    - subscription_id
    - api_endpoint_id
    - organization_id